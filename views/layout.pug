doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    nav
      div.container
          h2 Chess Casino
          ul.nav.justify-content-end.main-nav
            if user
              li.nav-item
                a.nav-link(href="#") Hello #{user.username}
              li.nav-item
                a.nav-link(href="/logout") Logout
            else
              li.nav-item
                a.nav-link(href="/register") Register
              li.nav-item
                a.nav-link(href="/login") Login
              li.nav-item
                img.level(src=`/images/eliteleague.png`)
                //img.level(src=`/images/${level}.png`)
    div.container
        block content

